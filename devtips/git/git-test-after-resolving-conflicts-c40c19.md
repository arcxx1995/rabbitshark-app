# Git: Test After Resolving Conflicts

**Category:** `git`  ·  **Format:** `shorts`

**Tags:** git, test, merge, conflicts, validation, commit, quality

## Steps

### 1. Run tests before final commit

```bash
$ npm test
✓ All tests passing
```

### 2. Check app starts without errors

```bash
$ npm start
Server running on :3000
```

### 3. Verify both features work together

```javascript
// Test the merged functionality
const result = auth.login(user);
const profile = api.getProfile(result.token);

console.log('Merge successful!');
```

### 4. Now commit with confidence

```bash
$ git commit -m "Merge: auth + api"
[main abc123] Merge: auth + api
```

---

_Generated by TubeForge._
